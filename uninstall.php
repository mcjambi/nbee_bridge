<?php

if (! defined('WP_UNINSTALL_PLUGIN')) {
    exit;
}

// Xóa dữ liệu liên quan đến plugin
// global $wpdb;
// $wpdb->query("DELETE FROM {$wpdb->prefix}options WHERE option_name LIKE 'my_plugin_%'");
// $wpdb->query("DELETE FROM {$wpdb->prefix}postmeta WHERE meta_key LIKE 'my_plugin_%'");
